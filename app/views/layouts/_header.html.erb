<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation-demo">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navigation-example-2">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="/">
        <div class="logo-container">
          <div class="logo">
            <img src="/PaperKitPro/assets/img/logo/logo-57x57-bw.png" alt="<%= APP_NAME %>" />
          </div>
          <div class="brand" style="font-size: 2em; padding-top: 10px;">
            <%= APP_NAME %>
          </div>
        </div>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
            <li><%= link_to "Announce an opening!", new_event_path, class: "btn btn-danger btn-fill" %></li>
        <% end %>
        <% if is_admin? %>
            <li class="dropdown">
              <button href="#" class="dropdown-toggle btn btn-warning" data-toggle="dropdown">Admin <b class="caret"></b></button>
              <ul class="dropdown-menu dropdown-warning dropdown-menu-right">
                <li><%= link_to "Activities and Event Types", activities_path %></li>
                <li><%= link_to "Venues", venues_path %></li>
                <li class="divider"></li>
                <li><%= link_to "Users", users_path %></li>
                <li class="divider"></li>
                <li><%= link_to "List of ALL Events", events_path %></li>
              </ul>
            </li>

        <% end %>

        <% if logged_in? %>
            <li class="dropdown">
              <button href="#" class="dropdown-toggle btn" data-toggle="dropdown">My Account <b class="caret"></b></button>
              <ul class="dropdown-menu dropdown-menu-right">
                <li><%= link_to "My Profile", current_user %></li>
                <li><%= link_to "My Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Log out", logout_path, method: :delete %>
                </li>
              </ul>
            </li>
        <% else %>
            <li><%= link_to "Log in", login_path, class: "btn btn-success" %></li>
        <% end %>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-->
</nav>
